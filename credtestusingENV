pipeline {
   agent any
        environment { 
            CREDS = credentials('TMCreds') 
        }
        stages {
           stage ('TMcredsTest'){
               steps {
               
               sh('curl -u $CREDS_USR:$CREDS_PSW https://deloitte-poc.threatmodeler.net/')
               }
             
           }
           
        }
}
