pipeline {
    agent any

    stages {
        stage('cred') {
            steps {
	      scripts {
               
		
		withCredentials([usernamePassword(credentialsId: 'TMcreds', passwordVariable: 'PWD', usernameVariable: 'USER')]) {
		
		sh '''#!/bin/bash -l
		echo "Hello World"
		println "${USER}"
		 
               echo "${PWD}"
      }         '''
}
		
            }
        }
    }
}
