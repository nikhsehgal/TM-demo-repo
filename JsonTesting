pipeline {
    agent any
    environment {
        def config = readJSON file: 'TM.json'
        projectId = "${config.TM\ pipeline}"
    }
    stages {
        stage('Test') {
            steps {
                echo projectId
            }
        }
    }
}
