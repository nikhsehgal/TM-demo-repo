pipeline {
    agent any
    environment {
        def config = readJSON file: 'TM.json'
        projectId = "${config.TMpipeline}"
    }
    stages {
        stage('Test') {
            steps {
                echo projectId
            }
        }
    }
}
